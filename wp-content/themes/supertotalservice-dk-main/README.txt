WordPress Theme: supertotalservice.dk-main
Generated by WPConvert.ai

INSTALLATION:
1. We recommend installing on a fresh WordPress site. If you are installing on
   an existing site, delete any existing pages and menus first (Pages → All Pages,
   Appearance → Menus) to avoid duplicate content. The theme automatically creates
   pages and menus on activation.
2. Log into your WordPress dashboard
3. Go to Appearance → Themes → Add New → Upload Theme
4. Select this ZIP file and click Install → Activate

⚠️ E-COMMERCE DETECTED (WooCommerce Recommended):
We detected shopping features on this site ().
These were built with custom JavaScript that does not transfer to WordPress.
Your content pages (about, blog, etc.) have been converted normally.

To restore shopping functionality:
1. Install WooCommerce from Plugins → Add New
2. Recreate your products in Products → Add New
3. Connect a payment gateway (Stripe, PayPal, etc.) under WooCommerce → Settings → Payments

SEO (YOAST & RANK MATH):
Out of the box (no plugin needed) this theme outputs your captured page titles,
meta descriptions, Open Graph tags, canonical URLs, and JSON-LD structured data.

To use an SEO plugin (optional, recommended):
1. Install Yoast SEO or Rank Math from Plugins → Add New (search the name),
   then Install → Activate. (There is NO separate WPConvert SEO plugin to upload.)
2. Run the plugin's setup wizard as you normally would.
3. Open a page under Pages → Edit. That first edit-screen open is when the
   theme auto-seeds the plugin, so the page's SEO data is populated.

What the theme does automatically once Yoast or Rank Math is active:
- Stands down its own meta tags so the plugin takes over (no duplicate tags).
- Feeds the plugin's content analyser your REAL page content (the WP post body
  is an intentional placeholder), so readability/SEO scores reflect actual text.
- Pre-fills the SEO title and meta description from your source site.
- Pre-fills a suggested focus keyphrase from the page's main heading.
- Flags the front page as Cornerstone (Yoast) / Pillar Content (Rank Math).
- Leaves sitemaps and breadcrumbs to the plugin (they work on your real pages).

Notes:
- Seeding runs once per field and NEVER overwrites a value you set yourself.
- If your source site had no meta description, the theme seeds one derived from
  the page's first paragraph — edit it anytime in the plugin's snippet editor.
- A blank focus keyphrase + red SEO score on a brand-new page is normal until
  the page is opened in the editor (which triggers the one-time seeding).

SET UP YOUR FORMS:
We detected 3 forms on this site. Your theme keeps the form layout, but this
conversion tier does not include the WPConvert Forms plugin (PRO, Agency, and
PAYG conversions bundle it for one-click form wiring).

To wire forms manually:
1. Install a form plugin (Contact Form 7, WPForms, Gravity Forms, or similar)
2. Recreate each form with the same fields and labels
3. Edit the relevant template (e.g. front-page.php or page-*.php) and replace
   the <form>...</form> block with the form plugin's shortcode

Upgrade to PRO at https://wpconvert.ai/pricing to get the WPConvert Forms
plugin and one-click "Wire All Forms" on your next conversion.

For more detailed instructions, visit: https://wpconvert.ai/support
